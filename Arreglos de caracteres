#include <stdio.h>
#include <stdlib.h>
#include <string.h>
int main()
{
    /*char Nombre[]=" Daniel Emmanuel";
    char Apellido[]=" Guzman Lomeli";
    char Completo[50];
    char Copia[50];

    printf("%s\n",Nombre);
    strcpy(Copia,Nombre);

    strcat(Nombre,Apellido);
    printf("%s\n",Nombre);


    strcpy(Completo,Nombre);
    printf("%s\n",Completo);


    strlen(Completo);
    printf("%d\n",strlen(Completo));

    strcmp(Completo,Copia);
    printf("%d\n",strcmp(Completo,Copia));
*/
char NCompleto[3][20]={" Daniel"," Guzman"," Lomeli"};

strcat(NCompleto[0],NCompleto[1]);
strcat(NCompleto[0],NCompleto[2]);
 printf("%s",NCompleto[0]);

    return 0;
}
